services:
  - type: web
    name: l2m-bossbot
    env: python
    plan: free
    pythonVersion: 3.12    # ← 追加（3.11でもOK）
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: DISCORD_TOKEN
        sync: false
